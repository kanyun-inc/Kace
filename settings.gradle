pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "Kace"

include(":kace-compiler")
include(":kace-gradle-plugin")
include(":kace-runtime")
